#!/bin/sh

# pwatchdogd reports health state transitions itself. Suppress the detailed
# error emitted on every polling interval while preserving the exit status.
exec /usr/sbin/fw-healthcheck 2>/dev/null
