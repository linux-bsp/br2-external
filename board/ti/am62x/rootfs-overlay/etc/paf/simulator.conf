# Standalone PAF protocol simulator. It opens SocketCAN directly.

[satellite]
interface = can1

[mcu_can]
interface = can0

[simulator]
log_enabled = false
log_file = /var/log/satellite-telemetry.log
timeout_ms = 2500
retries = 0
period_ms = 1000
